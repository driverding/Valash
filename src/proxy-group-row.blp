using Gtk 4.0;
using Adw 1;

template $ValashProxyGroupRow : Adw.ExpanderRow {
  [suffix]
  Gtk.Box {

  }

  Gtk.Button delay_check_button {
    label: _("Delay Check and Update");
    clicked => $on_delay_check_button_clicked ();
    margin-start: 8;
    margin-top: 8;
    margin-end: 8;
    margin-bottom: 8;
  }

  Gtk.FlowBox flow_box {
    homogeneous: true;
  }
}