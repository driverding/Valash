using Gtk 4.0;
using Adw 1;

template $ValashProxiesPage : Gtk.Box {
  Gtk.ScrolledWindow {
    Adw.Clamp {
      maximum-size: 600;
      margin-start: 16;
      margin-end: 16;
      margin-bottom: 16;

    }
  }
}