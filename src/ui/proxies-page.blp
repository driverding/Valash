using Gtk 4.0;
using Adw 1;

template $ValashProxiesPage : Gtk.Box {
  Gtk.ScrolledWindow {
    Adw.Clamp {
      maximum-size: 600;
      margin-start: 16;
      margin-end: 16;
      margin-bottom: 16;

      Gtk.Box {
        orientation: vertical;
        spacing: 16;

        Adw.PreferencesGroup proxies_group {
          title: _("Proxies");


        }

        Adw.PreferencesGroup proxy_providers_group {
          title: _("Proxy Providers");
        }

        Gtk.Box {
          orientation: horizontal;
          homogeneous: true;
          spacing: 12;
          Gtk.Button {
            label: _("Delay Check and Update All");
          }
        }
      }
    }
  }
}
